# Bug 复现说明

## Bug 是什么

并发新增订阅时存在丢失更新；列表查询返回的底层切片被调用方原地修改，导致筛选后其他记录消失。

## 如何触发

并发执行多次新增，同时调用列表查询；随后先按 active 筛选，再查询全部订阅。

```bash
CGO_ENABLED=0 go test ./...
```

## 错误信息

- 并发新增后总数少于预期。
- 按 active 筛选后，再查全部订阅会丢失 paused 记录。
