# Bug 复现说明

## Bug 是什么

参数校验与错误传播失效：非法状态被当作 active，请求体未知字段被接受，缺失订阅的更新请求最终返回 500 而不是 404。

## 如何触发

1. 创建订阅时在 JSON 中额外传入任意未知字段。
2. 请求 `GET /api/v1/subscriptions?status=invalid`。
3. 更新一个不存在的订阅 ID。
4. 运行完整测试：

```bash
CGO_ENABLED=0 go test ./... -count=20
```

## 错误信息

- 未知字段请求应返回 400，实际返回 201。
- invalid status 应返回 400，实际返回 200。
- 缺失订阅的更新应返回 404，实际返回 500。
